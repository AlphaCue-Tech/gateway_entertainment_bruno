info:
  name: Delete CV
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{local_base}}/api/v1/assets/cv/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
