info:
  name: own CV view
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_base}}/api/v1/assets/cv/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
