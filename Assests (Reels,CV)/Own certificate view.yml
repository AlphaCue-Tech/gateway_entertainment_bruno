info:
  name: Own certificate view
  type: http
  seq: 7

http:
  method: GET
  url: "{{local_base}}/api/v1/assets/certificates/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
