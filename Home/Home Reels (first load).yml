info:
  name: Home Reels (first load)
  type: http
  seq: 1

http:
  method: GET
  url: "{{local_base}}/api/v1/assets/feed/reels/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
