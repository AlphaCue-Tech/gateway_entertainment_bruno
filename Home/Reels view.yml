info:
  name: Reels view
  type: http
  seq: 3

http:
  method: GET
  url: "{{local_base}}/api/v1/assets/reels/21/view/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
