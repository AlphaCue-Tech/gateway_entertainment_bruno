info:
  name: Reels (Cursor)
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_base}}/api/v1/assets/feed/reels/?cursor=42&page_size=10"
  params:
    - name: cursor
      value: "42"
      type: query
    - name: page_size
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
