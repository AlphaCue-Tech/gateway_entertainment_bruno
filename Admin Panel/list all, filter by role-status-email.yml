info:
  name: list all, filter by role/status/email
  type: http
  seq: 6

http:
  method: GET
  url: "{{local_base}}/api/v1/admin/users/   "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
