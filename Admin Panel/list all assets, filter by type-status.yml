info:
  name: list all assets, filter by type/status
  type: http
  seq: 10

http:
  method: GET
  url: "{{local_base}}/api/v1/admin/assets/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
