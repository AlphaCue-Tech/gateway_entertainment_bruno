info:
  name: list all jobs, filter by status
  type: http
  seq: 12

http:
  method: GET
  url: "{{local_base}}/api/v1/admin/jobs/ "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
