info:
  name: List of all jobs
  type: http
  seq: 3

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/admin/jobs/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
