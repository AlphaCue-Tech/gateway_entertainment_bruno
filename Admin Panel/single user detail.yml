info:
  name: single user detail
  type: http
  seq: 7

http:
  method: GET
  url: "{{local_base}}/api/v1/admin/users/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
