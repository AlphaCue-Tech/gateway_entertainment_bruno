info:
  name: Disable Assest
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{local_base}}/api/v1/assets/admin/assets/1/disable/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
