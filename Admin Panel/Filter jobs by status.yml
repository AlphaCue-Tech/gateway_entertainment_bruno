info:
  name: Filter jobs by status
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/admin/jobs/?status=open"
  params:
    - name: status
      value: open
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
