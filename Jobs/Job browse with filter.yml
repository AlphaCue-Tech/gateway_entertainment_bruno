info:
  name: Job browse with filter
  type: http
  seq: 3

http:
  method: GET
  url: "{{local_base}}//api/v1/jobs/?skills=camera,lighting"
  params:
    - name: skills
      value: camera,lighting
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
