info:
  name: Reject an Applicant
  type: http
  seq: 14

http:
  method: PATCH
  url: "{{local_base}}/api/v1/jobs/applications/2/status/"
  body:
    type: json
    data: |-
      {
        "status": "rejected"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
