info:
  name: Job browse
  type: http
  seq: 2

http:
  method: GET
  url: "{{local_base}}//api/v1/jobs/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
