info:
  name: Self Job applications
  type: http
  seq: 9

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/my-applications/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
