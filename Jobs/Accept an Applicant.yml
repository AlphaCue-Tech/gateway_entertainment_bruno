info:
  name: Accept an Applicant
  type: http
  seq: 13

http:
  method: PATCH
  url: "{{local_base}}/api/v1/jobs/applications/1/status/"
  body:
    type: json
    data: |-
      {
        "status": "accepted"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
