info:
  name: Job detail update
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{local_base}}/api/v1/jobs/1/"
  body:
    type: json
    data: |-
      {
        "description": "Updated: Looking for a camera operator for a 5-day shoot.",
        "status": "open"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
