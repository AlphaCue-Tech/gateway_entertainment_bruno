info:
  name: View Applicants for a Job
  type: http
  seq: 10

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/2/applications/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
