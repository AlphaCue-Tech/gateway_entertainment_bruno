info:
  name: View Applicants Filter by Status
  type: http
  seq: 11

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/2/applications/?status=pending"
  params:
    - name: status
      value: pending
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
