info:
  name: Self posted jobs
  type: http
  seq: 7

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/my/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
