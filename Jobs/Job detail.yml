info:
  name: Job detail
  type: http
  seq: 4

http:
  method: GET
  url: "{{local_base}}/api/v1/jobs/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
