info:
  name: Password reset
  type: http
  seq: 8

http:
  method: POST
  url: "{{local_base}}/api/v1/auth/password-reset/"
  body:
    type: json
    data: |-
      {
        "email": "crew@email.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
