info:
  name: Crew Prodile PUBLIC
  type: http
  seq: 7

http:
  method: GET
  url: "{{local_base}}/api/v1/profiles/crew/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
