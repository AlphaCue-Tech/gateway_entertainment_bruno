info:
  name: Registration
  type: http
  seq: 2

http:
  method: POST
  url: "{{gateway_base}}/api/v1/auth/register/"
  body:
    type: json
    data: |-
      {
        "email" : "employer2@email.com",
        "password" : "passworxd",
        "role" : "employer",
        "preferred_language" : "en",
        "full_name" : "Employer Full Name 2",
        "company_name": "AlphaCUe Technologies"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
