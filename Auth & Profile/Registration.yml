info:
  name: Registration
  type: http
  seq: 2

http:
  method: POST
  url: "{{local_base}}/api/v1/auth/register/"
  body:
    type: json
    data: |-
      {
        "email" : "crew@email.com",
        "password" : "passworxd",
        "role" : "crew",
        "preferred_language" : "en",
        "full_name" : "Crew Full Name",
        "company_name": "AlphaCUe Technologies"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
