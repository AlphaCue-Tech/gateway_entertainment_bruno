info:
  name: Password reset Confirm
  type: http
  seq: 9

http:
  method: POST
  url: "{{local_base}}/api/v1/auth/password-reset/confirm/"
  body:
    type: json
    data: |-
      { "uid": "", 
       "token": "", 
       "new_password": "" }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
